# Quality Check (MVP)

Enthält einfache Checks für Layout-JSON:

- `layout_validator.py`: Schema + Bounds Checks
- `preflight_checker.py`: Preflight-Aggregation (MVP)
- `amazon_checker.py`: delegiert an `variant_generator.amazon_validator`

